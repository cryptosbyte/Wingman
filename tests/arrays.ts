import * as Arrays from "../lib/arrays";

/*
    Testing RandomIndex method to get a random index from an array.
    // console.log(Arrays.RandomIndex(["some", "cool", "project", "poggers"]));
*/

/* 
    Testing FitlerExact method to get the total amount of exact appearances of 1 inside of an array.
    // console.log(Arrays.FilterExact([1, 6, 2, 1, 1, 1], 1));
*/

/*
    Testing LoopFunction method to append a string with "a" 4 times.
    // let Str = "";
    // Arrays.LoopFunction(4, () => (Str += "a"));

    // console.log(Str);
*/


/*
    Testing ArrayToObject method to convert an array to an object
    // console.log(Arrays.ArrayToObject(["test", "b", 5]))
*/