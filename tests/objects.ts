import * as Objects from "../lib/objects";

/*
    Testing ReverseObjectKeyValue method to convert the given object's value as the key and the original key as the value.
    // console.log(Objects.ReverseObjectKeyValue({"cool":"bye", 12:12, "lol": 62, 1: "lol"}))
*/

/*
    Testing GetValueFromPath method to get the value of the path given (converts into a dot notation path to check if it's inside of the object)
    // let myObj = { nice: 1, poggers: { test: "ok", yep: true } }
    // console.log(Objects.GetValueFromPath(myObj, "poggers.test"))
*/
